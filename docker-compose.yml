version: '3.8'
services:
  app:
    build: .
    ports:
      - '8000:8000'
    restart: unless-stopped

  # If you prefer separate containers, uncomment the frontend service below
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - '5173:80'
  #   depends_on:
  #     - app
  #   restart: unless-stopped
