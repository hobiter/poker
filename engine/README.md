# Engine scaffold

This directory contains a minimal engine scaffold used during Phase 1.

Files:
- `deck.py` — deterministic deck with seeded shuffle and dealing helpers.
- `test_deck.py` — unit tests using Python's `unittest` (no external dependencies).

Run tests locally:

```bash
python -m unittest -v engine.test_deck
```
